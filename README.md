# README.md

This repo contains PDF and Microsoft Word (docx), formatted versions of my resume generated by a Node script from Markdown files in a corresponding private repository. The current base resume Markdown file is also published here.

The built files are ATS-compliant.
